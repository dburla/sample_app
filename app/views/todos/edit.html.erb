<% provide(:title, "Edit Todo") %>

<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>Edit Todo</h1>
      
      <%= form_for(@todo, local: true) do |f| %>
        <% if @todo.errors.any? %>
          <div class="alert alert-danger">
            <h4><%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved:</h4>
            <ul>
              <% @todo.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        
        <div class="form-group">
          <%= f.label :text, "Todo Description" %>
          <%= f.text_area :text, class: "form-control", rows: 4 %>
        </div>
        
        <div class="form-group">
          <div class="checkbox">
            <label>
              <%= f.check_box :completed %>
              Mark as completed
            </label>
          </div>
        </div>
        
        <div class="form-group">
          <%= f.submit "Update Todo", class: "btn btn-primary" %>
          <%= link_to "Cancel", todos_path, class: "btn btn-default" %>
        </div>
      <% end %>
    </div>
  </div>
</div>